# Server-side secret (do NOT commit real keys)
GEMINI_API_KEY=

# Optional: client-side key for local dev only (fallback)
VITE_GEMINI_API_KEY=

# Model to use (optional, defaults in code)
VITE_GEMINI_MODEL=gemini-1.5-flash
